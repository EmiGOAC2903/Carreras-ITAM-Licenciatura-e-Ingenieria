# Comandos utilizados

```bash
# Clonar el repositorio
git clone https://github.com/EmiGOAC2903/Carreras-ITAM-Licenciatura-e-Ingenieria
cd Carreras-ITAM-Licenciatura-e-Ingenieria

# Crear rama de trabajo
git checkout -b pagina

# Agregar archivo index.html
git add index.html
git commit -m "Agregar index.html en la rama del proyecto"

# Subir la rama al remoto
git push -u origin pagina

# Crear Pull Request en GitHub: pagina -> main

# Crear rama para el README
git checkout -b pagina

# Agregar README.md
git add README.md
git commit -m "Añadir README con comandos"

# Subir la rama al remoto
git push -u origin pagina


